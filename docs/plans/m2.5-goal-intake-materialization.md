# M2.5 Goal Intake and Materialization implementation plan

- Status: In progress; Slices 1 through 3 are complete; the product loop remains
  unimplemented and Slice 4 has not begun
- Plan date: 2026-08-02
- Milestone: M2.5
- Milestone boundary: [CodeClosure milestones](../milestones.md#m25--goal-intake-and-materialization-vertical-slice)
- Milestone assessment: [M2.5 acceptance plan](m2.5-acceptance-plan.md)

## 1. Purpose and completion meaning

This plan translates the accepted M2.5 Goal Intake contract into a bounded
implementation order. It incorporates the implementation-readiness items
reviewed after the M2 exit verdict: Intake persistence, the Intake Assistant
Adapter, Thread policy, deterministic Admission, Materialization and Start
composition, CLI and read views, compatibility, recovery, and adversarial
proof.

This document does not create a second product contract. If it conflicts with
the Runtime invariants, an accepted ADR, the product/architecture contracts, or
[Goal Intake](../goal-intake.md), the higher-authority record wins and
implementation MUST stop until the conflict is resolved.

Completing this document means only that M2.5 has a build and proof sequence.
It does not mean that Goal Intake is implemented, that M2.5 has passed, that a
materialized Goal is technically complete, or that CodeClosure is authorized
to merge, release, deploy, communicate, purchase, or perform another external
effect.

## 2. Governing authority

The implementation MUST preserve, in order:

1. [`RUNTIME_INVARIANTS.md`](../../RUNTIME_INVARIANTS.md), including one
   Workflow writer, one technical Acceptance issuer, atomic authority writes,
   conversation-independent recovery, fail-closed unknown state, and bounded
   retry authority;
2. [ADR 0027](../adr/0027-source-bound-intent-admission-and-automatic-goal-materialization.md),
   which owns source-bound Intent Admission, terminal Intake failure, atomic
   Materialization, and the separate Start boundary;
3. [ADR 0034](../adr/0034-close-pre-goal-command-replay-and-sqlite-activation.md),
   which owns pre-Goal command reservation/outcome replay and retained Intake
   project participation in verified SQLite activation;
4. the accepted M1 and M2 ADRs governing command identity, Store-authored
   outcomes, canonical digests, causal time, strict reopen, the public Runtime
   facade, immutable Workflow Policy/Profile binding, and the reusable App
   Server client;
5. [`PRODUCT.md`](../../PRODUCT.md) and
   [`ARCHITECTURE.md`](../../ARCHITECTURE.md);
6. [Goal Intake](../goal-intake.md), [Domain Model](../domain-model.md),
   [Workflow](../workflow.md), [Context](../context-compiler.md), and
   [Evidence](../evidence-model.md); and
7. the [M2.5 milestone boundary](../milestones.md#m25--goal-intake-and-materialization-vertical-slice).

The completed [M1](m1-deterministic-skeleton.md) and
[M2](m2-codex-vertical-slice.md) plans remain regression evidence. They do not
become Intake authority and M2.5 MUST NOT reinterpret a Worker Thread, Turn,
external-execution record, Candidate, Evidence, or Acceptance record as
pre-Goal intent.

## 3. Entry conditions

Implementation MUST NOT begin until all of the following are true:

1. the [M2 completion review](../reviews/m2-completion-review.md) remains an
   unconditional `PASS` for the exact bounded M2 source identity;
2. this implementation plan and the independent M2.5 acceptance plan are
   committed and linked from the repository plan and milestone indexes;
3. the complete M1 and M2 regression baselines are green on the implementation
   source identity;
4. Node and pnpm satisfy the root manifest and the lockfile is current;
5. the implementation branch records its base revision and declares any
   pre-existing working-tree changes;
6. no higher-authority contract conflict is being waived; and
7. Slice 0 has fixed the exact installed Intake Admission Policy, Intake
   assistant profile, Workflow Policy, and Execution Profile identities used by
   the bounded governed-execution proof.

Planning work and deterministic read-only probes MAY occur before product code.
They MUST NOT be reported as implementation or milestone completion.

## 4. Fixed scope

The bounded implementation includes:

- immutable Raw Request roots and revisions with exact admitted user content,
  trusted local principal, explicit interaction action, project/scope and
  declared constraints;
- a versioned IntakeRun lifecycle with immutable Proposal, Projection, Source
  Binding, Material Ambiguity, Clarification Question, Clarification Answer
  Binding, Decision, Answer-only, Failure, Materialization, and optional
  Start-Authorization authority;
- a distinct Goal Intake Assistant Adapter over the reusable App Server client;
- separate closed response contracts for Intent analysis and Answer-only;
- a deterministic installed Intent Admission Policy and Engine;
- atomic `CLARIFY`, `NO_EXECUTION`, and `MATERIALIZE` transactions;
- terminal Intake processing failure and restart reconciliation without model
  recall;
- exact pre-Goal command reservation, idempotency, replay, conflict, and
  concurrent-loser behavior;
- atomic Goal revision 1 plus unique `DISCOVERY / READY` Workflow creation;
- optional Goal Start Authorization followed only by a separate ordinary
  `StartGoal` command;
- basic CLI submission, clarification, status, and audit views;
- strict SQLite migration, immediate-read, close/reopen, and corrupted-state
  validation; and
- the full M2.5 deterministic and adversarial acceptance matrix.

The implementation explicitly excludes:

- a long-lived chat or `InteractionSession` router;
- front-door routing among ordinary chat, Intake, Goal status, and control
  requests;
- a front-end Thread epoch or conversation-memory subsystem;
- optional Intake project exploration in the baseline slice;
- broad project/business discovery or full Fact Graph traversal;
- Goal revision after Materialization;
- multiple simultaneous Clarification Questions, batch answers, Question
  lifecycle state machines, or a generic questionnaire engine;
- rich TUI, complete Human Decision UX, Goal scheduling, multiple Intake
  agents, model voting, or multi-Goal Worker concurrency;
- automatic retry of a failed Intake operation;
- technical Acceptance, Candidate Promotion, or external-effect changes; and
- any new M2.6 milestone claim. A later unified front door requires its own
  accepted repository decision and is not inferred from this plan.

## 5. Baseline implementation decisions

### 5.1 Trusted interaction surface

The initial CLI MUST require one visible, explicit action for every new Raw
Request:

- `ANSWER_ONLY`;
- `MATERIALIZE_ONLY`; or
- `GOVERNED_EXECUTION`.

M2.5 defines no implicit CLI default. Conversation continuation, inactivity,
model classification, a detached approval, or user silence cannot supply the
action. The single-user local CLI obtains its principal identity from trusted
composition; it does not accept a caller-supplied `--principal` override.

The planned command surface is:

```text
codeclosure intake submit \
  --action <answer-only|materialize-only|governed-execution> \
  --request <text> [--project <path>] [--constraint <text> ...] [--json]

codeclosure intake clarify <intake-run-id> \
  --question-id <clarification-question-id> \
  --expected-version <number> --answer <text> [--project <path>] [--json]

codeclosure intake abandon <intake-run-id> \
  --expected-version <number> [--json]

codeclosure intake status <intake-run-id> [--json]
codeclosure intake audit <intake-run-id> [--json]
```

`intake submit` and `intake clarify` invoke one Runtime-owned Intake operation
and return its stored or current typed result. The CLI MUST NOT expose a raw
`materialize` command that can consume a stale derived
`READY_TO_MATERIALIZE` view. The deterministic `MATERIALIZE` Decision and Goal
authority commit together.

`intake clarify` accepts no caller-restated question data beyond the supplied
Question ID; Runtime resolves its retained `questionSpecDigest`, exact
`questionDigest`, issuing Decision binding, and answer schema and binds them
into command input. Runtime accepts it only when that question belongs to the
Intake Run, is the single current unanswered question, remains bound to current
authority, and its answer schema accepts the supplied input. M2.5 emits exactly
one active question per `CLARIFY`; it supports neither positional nor batch
answers.

An accepted clarification atomically creates a new Raw Request revision whose
semantic digest embeds the exact Question/Decision tuple, one unique immutable
`ClarificationAnswerBinding` that additionally binds the revision and command,
and a cleared singular `activeQuestionRef`. The Question is never mutated and
historical bindings are queried separately from current IntakeRun state.

`intake clarify --project` is permitted only as structured user input when that
exact question allows project identity to be established or corrected. Trusted
composition normalizes the path and adds it to the isolation lease before Store
activation; retained project roots remain in that lease. After activation,
Runtime validates the exact Intake ID, expected version, principal, question
ID, both question digests, issuing Decision, canonical command digest, and
normalized path before committing a new Raw Request revision; that command
digest includes the normalized path when present. Free-form `--answer` text and
assistant output cannot introduce project-path authority, and status/audit
commands accept no project override. A stale, foreign, already answered,
mismatched, or unrelated question produces no Raw Request revision, lifecycle
mutation, or assistant invocation; the
deterministic admitted-rejection transaction still records the exact
reservation, `REJECTED` outcome, observed version, and audit.

`intake abandon` is the only local-v1 source of trusted `ABANDONED`. It is
accepted only for the exact expected version of a `NEEDS_CLARIFICATION` Run
with one current unanswered Question and no external operation in flight. One
transaction creates the exact `ABANDON_CLARIFICATION` command reservation;
resolves and binds the current Projection, Question specification/record
digests and issuing `CLARIFY` Decision in one complete
`AbandonClarificationReservationBinding`; binds the Command ID and canonical
command-input digest; clears `activeQuestionRef`; and persists
`PROJECTED_NO_EXECUTION / ABANDONED`, terminal `NO_EXECUTION`, `APPLIED`
outcome, and audits. It creates no Raw Request revision and makes no assistant
call. After schema, target, and replay-integrity admission, an `ANALYZING`,
terminal, stale, foreign, or otherwise ineligible Run commits only the ADR 0034
base reservation plus deterministic `REJECTED` outcome and audit without the
binding, a Decision, or changed Intake lifecycle authority. Invalid input, a
genuinely missing target, command conflict, or replay-integrity failure creates
no replacement row.

The reservation-only binding is exactly:

```text
IntakeCommandReservation
  operationKind = ABANDON_CLARIFICATION
  abandonClarificationBinding?

AbandonClarificationReservationBinding
  clarificationQuestionId
  questionSpecDigest
  questionDigest
  issuingClarifyDecisionId
  issuingClarifyDecisionDigest
```

The required embedded Decision value is exactly:

```text
AbandonmentBinding
  clarificationQuestionId
  questionSpecDigest
  questionDigest
  issuingClarifyDecisionId
  issuingClarifyDecisionDigest
  commandId
  canonicalCommandInputDigest
```

It is required only on local-v1 `PROJECTED_NO_EXECUTION / ABANDONED` and is
forbidden on every other Decision kind or reason. Local-v1 abandonment therefore
cannot use `PRE_ANALYSIS_NO_EXECUTION`; a future policy would require a new
trusted abandonment source, Policy version, and plan review.

Direct `goal create` remains unchanged. It MUST NOT synthesize Raw Request,
Proposal, Projection, Admission, Materialization, or Start-Authorization
history.

### 5.2 Package and dependency boundaries

The implementation uses the existing workspace and one new adapter package:

| Location | Planned ownership | Prohibited authority |
| --- | --- | --- |
| `packages/domain` | Intake identifiers, closed records, codecs, invariants, canonical projections | Codex DTOs, process control, Store access, model calls |
| `packages/runtime` | Intake application facade, Coordinator, package compiler, Admission Engine, policy installer, ports, transaction planning, read views | Codex DTOs, direct SQL, model-authored authority |
| `packages/store-sqlite` | Intake schema, compound transactions, queries, audit, strict reopen and migration backstops | issuing Admission or Goal intent, calling the assistant |
| `packages/codex-app-server-client` | protocol/process/stream primitives already proven by M2 | Domain, Runtime, Store, Worker, Intake, Candidate, Evidence or Acceptance imports |
| `packages/adapter-codex-intake` | `IntakeAssistantPort` implementation and protocol translation | `WorkerPort`, Candidate/source write, Store, Goal Manager, Workflow, Start, Evidence, Acceptance or external effects |
| `apps/cli` | trusted action capture, project normalization, narrow composition and rendering | raw Store/kernel access, lifecycle inference, SQL, assistant classification as action |
| `packages/testing` | deterministic policies, IDs, clocks, App Server fixtures and adversarial data | production authority or completion claims |

The Worker and Intake adapters MAY share only the lower App Server client. They
MUST NOT import one another or share Worker/Intake domain request types. Static
dependency tests MUST prove both directions, and the lower client MUST remain
free of `@codeclosure/*` semantic dependencies.

### 5.3 Pre-Goal command identity and idempotency

Existing `processed_commands` authority is Goal/Workflow-bound and MUST NOT be
made to target a fabricated Goal before Materialization. Under
[ADR 0034](../adr/0034-close-pre-goal-command-replay-and-sqlite-activation.md),
M2.5 adds a distinct Intake command contract with two immutable record kinds:

```text
IntakeCommandReservation
  -> one exact canonical command input has claimed an Intake operation

IntakeCommandOutcome
  -> one exact APPLIED | REJECTED | FAILED final result for that reservation
```

Every reservation binds `CommandId`, closed operation kind, trusted principal,
target RawRequest/IntakeRun, canonical input digest, expected and observed
Intake version when applicable, operation identity, causal time, and one
canonical `reservationDigest`. For external work it also binds the exact
Intake/Answer-only Manifest ID/digest, installed Admission Policy, adapter, and
response-contract identities. It commits with the initial Raw Request/IntakeRun
authority, exact operation Manifest/bindings, and audit before an assistant
call. A separate Store-authored immutable outcome binds that exact Reservation
digest plus the final Intake records, audit, status, safe result projection,
and disposition afterward.

A clarification reservation additionally binds the exact
`ClarificationQuestionId`, retained `questionSpecDigest`, exact
`questionDigest`, issuing Decision ID/digest, and answer schema. The caller
supplies only the Question ID and cannot restate those retained bindings. M2.5
permits exactly one current unanswered question; question order is never part
of command meaning.

The reservation transaction also persists the new Raw Request revision with
its embedded `AnsweredQuestionBinding`, a unique immutable
`ClarificationAnswerBinding`, the cleared `activeQuestionRef`, updated
IntakeRun version/status, exact Manifest/external-operation bindings, and audit
before any fresh analysis call.

`ABANDON_CLARIFICATION` has exactly two terminal compound reservation shapes:

- the eligible `APPLIED` shape binds the exact current Question
  ID/specification/record digests and issuing `CLARIFY` Decision ID/digest in
  one complete `abandonClarificationBinding`, in addition to every base
  reservation field defined above. Its transaction creates no revision or
  external-operation binding; it commits the reservation, projected
  abandonment Decision carrying a complete `AbandonmentBinding`, cleared active
  reference, terminal Intake state, `APPLIED` outcome, and audits; and
- the deterministic `REJECTED` shape contains only the base reservation fields
  including observed Intake version. It forbids
  `abandonClarificationBinding`, any abandonment Decision, active-reference or
  lifecycle mutation, and external work.

`FAILED` is not a valid abandonment disposition, and no
`ABANDON_CLARIFICATION` reservation may exist without its outcome from the same
transaction. Infrastructure failure before that transaction commits creates
neither record.

A deterministic admitted rejection commits its reservation, `REJECTED`
outcome, observed Intake version, and audit together without a Raw Request
revision, Intake lifecycle mutation, or external call. Invalid input, missing
target, replay-integrity failure, and command conflict are not stored as
admitted rejections.

`APPLIED` includes `CLARIFY`, intentional `NO_EXECUTION`, successful
Materialization, and `NO_EXECUTION / ANSWER_FAILED`. `REJECTED` stores only a
deterministic admitted command rejection. `FAILED` requires one successfully
committed terminal Intake Failure Record; Store, audit, codec, clock,
replay-integrity, command-conflict, or unrecorded adapter failures do not become
stored `FAILED` outcomes.

Exact duplicate delivery MUST NOT call the assistant twice:

- replay of a final entry returns the stored Intake result without new Intake,
  Admission, Goal, Workflow, Materialization, Start-Authorization, or Intake
  audit authority;
- an active matching reservation returns a typed non-authoritative
  `IN_PROGRESS` view or joins the same in-process operation without creating
  another effect;
- the same `CommandId` with another input digest conflicts;
- a concurrent loser cannot create a second Raw Request revision, Proposal,
  Decision, Goal, or Start Authorization; and
- a retained orphan reservation is reconciled during startup before any new
  assistant call.

The Intake contract is not a Workflow command journal, Worker receipt,
Acceptance Decision, or completion authority. The separately preallocated
ordinary `StartGoal` still uses `processed_commands`; an Intake outcome cannot
replace it. Any implementation that diverges from ADR 0034 or weakens ADRs 0009
through 0013 MUST stop for another ADR.

After replay of a materialized `AUTHORIZE_START` result, the coordinator may
separately resubmit only that exact Start command. The ordinary command boundary
alone decides whether the missing first Start applies, an existing result
replays, or the command is rejected.

### 5.4 Persistence and migration profile

M2.5 adds one or more additive migrations beginning after current migration
`0025`. The migrations MUST introduce complete authority rather than placeholder
tables. They MUST implement ADR 0034's separate immutable reservation/outcome
records and include:

- Raw Request roots and immutable revisions;
- IntakeRun current state and optimistic version;
- Intake command reservations/outcomes;
- Intake Manifests and operation bindings;
- Proposals and Projection revisions;
- Source Bindings, Material Ambiguities, Clarification Questions, and
  Clarification Answer Bindings;
- installed Admission Policies and installation audits;
- closed Admission Decision variants;
- Answer-only Responses and Intake Failure Records;
- Goal Materialization Records and optional Goal Start Authorizations; and
- Intake-owned audit/query relationships.

The Store MUST enforce immutable rows, parent chains, exact digest bindings,
terminal-reference unions, action/outcome/disposition combinations, uniqueness
of one Materialization per admitted Projection, and at most one exact Start
Authorization for `AUTHORIZE_START`. For `CLARIFY`, it MUST independently
recompute the question-spec, Decision, and exact Question-record digests;
validate every repeated spec field and Decision/Question cross-reference; and
commit the Decision, Question, single active reference, Intake transition,
audits, and command outcome atomically.

For an accepted clarification, the Store MUST also recompute the new Raw
Request and Answer Binding digests; validate their exact Question, Decision,
parent revision, Intake, Command, and current-reference relationships; permit
only one Answer Binding per Question; clear the singular active reference; and
persist that whole answer boundary atomically. Missing, duplicate, cross-Intake,
or digest-mismatched Answer Bindings, an active already-answered Question, and
any `NEEDS_CLARIFICATION`/`ANALYZING`/terminal active-reference shape mismatch
fail strict reopen.

For accepted abandonment, the Store MUST recompute the new Decision digest and
validate the complete `AbandonmentBinding` against the exact active Question,
its specification/record digests, issuing `CLARIFY` Decision, current
Projection, complete `AbandonClarificationReservationBinding`, Command ID/input
digest, principal, and expected/observed Intake version. The reservation,
terminal Decision, cleared active reference, `APPLIED` outcome, and audits
commit atomically. A deterministic `REJECTED` abandonment instead MUST contain
only the base reservation plus outcome/audit and no binding, Decision, or Intake
mutation. Missing, partial, substituted, pre-analysis, reservation-less,
outcome-less, `FAILED`, or mixed-shape abandonment authority fails immediate
read and strict reopen.

Migration preflight MUST reject retained placeholder, malformed, partial,
cross-run, mixed-terminal, duplicate-materialization, or unprovable Intake
authority without partial schema application. Store writes MUST immediately
read through the owning codecs, and close/reopen MUST reconstruct the exact
authority chain. SQLite constraints are defense in depth and do not become a
second Admission, Goal, or Workflow owner.

Verified SQLite activation MUST include every retained Intake project/scope
reference and every explicit invocation root under ADR 0034, including a
structured project correction supplied to `intake clarify`. Retained roots are
never replaced by caller input. Those bootstrap paths are denial inputs only;
they do not become Raw Request, Source Binding, Admission, Goal, or recovery
authority. Runtime may bind the correction only after activation and exact
question ID, question-spec/record digests, issuing Decision, and version
validation.

### 5.5 Canonical identity and causal time

Slice 1 MUST add named schema-versioned ADR 0006 projections and golden vectors
for every Intake digest. At minimum this includes Raw Request revision, Intake
Manifest, Proposal, Projection, Source Binding, Material Ambiguity set,
Clarification Question specification and record, Clarification Answer Binding,
Admission Policy, Admission Decision including `AbandonmentBinding`,
`AbandonClarificationReservationBinding`, Answer-only Response, Failure Record,
Materialization Record, Start Authorization, and Intake command
reservation/outcome/input/result.

Record IDs, Runtime-authored observation timestamps, and each digest field are
excluded only where the owning contract declares them envelope metadata.
Semantic arrays use fixed canonical ordering. Exact admitted text is hashed as
UTF-8 bytes and the Raw Request revision separately binds the whole semantic
record. A text digest cannot substitute for a revision digest.

The acyclic `CLARIFY` projections are computed in order: first the
`ClarificationQuestionSpec.questionSpecDigest`; then the Admission Decision,
whose semantic digest includes that spec digest but excludes generated IDs;
then `ClarificationQuestion.questionDigest`, whose exact relationship
projection includes its Question ID, issuing Decision ID/digest, repeated spec,
and spec digest. The Decision MUST NOT include `questionDigest`. Golden vectors
and Store poisoning tests cover every substitution edge.

The later abandonment Decision is not part of that acyclic Question
construction. Its semantic projection includes the already complete Question
specification/record digests, issuing `CLARIFY` Decision ID/digest, and exact
abandonment Command ID/input digest. It cannot rewrite the Question or the
earlier Decision.

For a clarification answer, the Raw Request semantic projection first includes
the exact embedded `AnsweredQuestionBinding`. The later
`answerBindingDigest` includes that Raw Request revision/digest plus the exact
Raw Request ID, Question, Decision, Command ID, and canonical command-input
digest; it excludes the generated Answer Binding ID, Runtime-authored answer
time, and itself. The Raw Request projection MUST NOT include the later Answer
Binding identity or digest. Raw Request, Question, Decision, and Command IDs
inside these projections are governed relationship identities; only the
generated Answer Binding record ID is envelope metadata.

Runtime time obeys the current causal-floor contract. Proposal observation,
Clarification Answer Binding, Decision, Failure, Materialization, Start
Authorization, audit, and command times cannot predate their source authority.
In particular, `answeredAt` cannot predate its Question, reservation, or Raw
Request revision. Time-sensitive Admission receives an explicit source-bound
observation; it does not read ambient wall time while claiming deterministic
replay.

### 5.6 Intake Assistant and Thread policy

The baseline selects the narrowest enforceable policy under
[ADR 0035](../adr/0035-bound-intake-by-non-authoritative-effects.md):

1. each Intent-analysis or Answer-only operation starts one controlled App
   Server process, one fresh physical Thread, and one bounded Turn;
2. M2.5 never resumes or reuses a previous Intake Thread;
3. a clarification answer creates a Question-bound Raw Request revision, a
   unique immutable Answer Binding, and a new operation whose fresh Thread
   receives the complete current Intake Package and Intake Manifest;
4. Answer-only uses a distinct `AnswerOnlyPackage` and response schema and
   receives no CodeClosure project-observation or other authority capability;
5. configurable Web Search, MCP, apps, plugins, hooks, skills, subagents,
   dynamic tools, shell/exec, Candidate writes, ambient project instructions,
   and external integrations are disabled for the Intake assistant profile;
6. the process cwd and state root are CodeClosure-owned, operation-scoped
   locations outside the project, Candidate, authority database, credential,
   and every other trusted forbidden root, and the Turn is read-only with
   network disabled and approval policy `never`;
7. the profile's empty `selectedAuthorityCapabilities` describes CodeClosure
   grants rather than the App Server's model-visible tool inventory; a pinned
   App Server built-in tool may be visible, but any observed tool Item
   interrupts and fails the complete operation and none of its output becomes
   Intake authority;
8. unexpected Compact, Thread replacement/loss, process exit, interruption,
   malformed stream, unsupported server request, missing terminal response, or
   response-schema mismatch fails that operation; and
9. process and Thread release is attempted at every terminal boundary, but
   neither retained conversation state nor successful cleanup becomes Intake
   authority.

The baseline does not claim that no upstream tool was selected before
notification. Read-only isolation, network denial, absent CodeClosure authority
ports, and strict output validation are the enforcement boundary; observed tool
rejection is additional containment.

There is no automatic replacement Thread or assistant retry. A successfully
recordable Intent-analysis failure closes the IntakeRun as `FAILED` with a
closed reason. A process crash before that compound commit leaves `ANALYZING`
authority for operation-kind-aware startup reconciliation: Intent analysis
closes as `FAILED / INTERRUPTED_ANALYSIS`, while Answer-only closes as
`APPLIED / NO_EXECUTION / ANSWER_FAILED / INTERRUPTED_ANSWER_DELIVERY` without
another assistant call. A user-requested retry starts a new IntakeRun.

This fresh-operation policy is an M2.5 implementation choice, not a product
claim that later milestones can never retain a Thread. A future reuse policy
must still recompile the current Intake Package and cannot make conversation
history authoritative.

### 5.7 Intake packages and response contracts

The Runtime compiler creates immutable operation input from exact persisted
authority. `IntakePackage` contains only the current Raw Request chain,
optional current Projection, clarification state, declared project/scope,
Intake/Admission Policy and adapter identities, response-contract identity,
response schema, and hard budgets. `AnswerOnlyPackage` contains only the exact
Raw Request binding, prepared `NO_EXECUTION / ANSWER_ONLY` Decision binding,
exact Admission Policy ID/version/digest, adapter ID/version,
response-contract digest, and output budgets.

The durable Intake Manifest records every included or intentionally omitted
input, its revision, provenance, digest, and budget decision. Neither package
may contain fabricated Goal, Workflow, Attempt, Worker Session, Candidate,
Evidence, Acceptance, Workflow Policy binding, or Execution Profile binding.

The assistant returns only closed untrusted values:

- Intent analysis returns one bounded `IntentAnalysisProposal`; and
- Answer-only returns bounded content or a typed adapter failure observation.

The Coordinator, not the adapter, allocates record identity, classifies safe
failure, validates source spans, constructs Source Bindings and ambiguities,
computes digests, and persists authority. Unknown fields, raw exceptions,
private reasoning, transcript content, observed tool attempts or results,
model-authored IDs, and
oversized output never enter authoritative records or audit.

### 5.8 Optional project observation is deferred

The basic M2.5 implementation does not add the optional project-exploration
port. The Intake assistant receives no project tool capability and cannot
produce `PROJECT_OBSERVED` authority in the baseline.

A request that requires governed repository inspection MUST NOT be classified
as Answer-only merely because it is read-only. When its source-bound formal
intent is otherwise admissible, M2.5 materializes a formal read-only Goal whose
later `DISCOVERY` phase owns the inspection. Project-assisted Intake remains an
M3 candidate under the existing exact-root, read-only capability, budget,
provenance, omission, and revalidation requirements.

### 5.9 Deterministic Admission Policy

Trusted composition installs one immutable bounded local Admission Policy from
a definition without a caller-authored digest. Runtime computes its canonical
identity; Store independently recomputes it and commits the policy plus
installation audit atomically.

The policy MUST define:

- required formal Goal fields and at least one explicit required Criterion;
- permitted source classes for each material field;
- meaning-preserving derivation rules;
- ambiguity classification and bounded question ordering;
- exactly one active question per M2.5 `CLARIFY` result;
- action/outcome/execution-disposition combinations;
- `ANSWER_ONLY`, `POLICY_DENIED`, `UNSUPPORTED`, and `ABANDONED` aggregation;
- exact Workflow Policy/Profile preflight for `AUTHORIZE_START`; and
- a stable ordered reason trace.

Fixed canonical input and policy produce the same outcome, reason trace, and
Decision digest. The Admission Engine has no assistant-call, Goal-write,
Workflow-write, Store-write, Worker, Candidate, Acceptance, or external-effect
capability.

For `GOVERNED_EXECUTION`, trusted composition selects the exact installed
Workflow Policy and Execution Profile. The CLI and assistant do not select or
override them. Missing or incompatible configured Start authority produces a
typed Intake preparation failure; it MUST NOT silently downgrade to
`MATERIALIZE_ONLY` or create a Goal without the promised governed-execution
causality.

### 5.10 Answer-only, failure, and recovery

A deterministic preflight MAY produce `PRE_ANALYSIS_NO_EXECUTION` without an
Intent-analysis call. For `ANSWER_ONLY`, a separate bounded assistant operation
then returns `ANSWER_RETURNED` or a safe `ANSWER_FAILED` reason. The final
compound transaction stores the Decision, Answer-only Response, terminal
`NO_EXECUTION`, audit, and command result together.

When deterministic preflight is dispositive and no Answer-only or other
external operation is required, one transaction stores any admitted Raw
Request revision, current Intake authority, command reservation, closed
`NO_EXECUTION` Decision, terminal status, `APPLIED` command outcome, and audits.
It does not create an intermediate `ANALYZING` operation or split the already-
known result across transactions.

Answer delivery failure remains `NO_EXECUTION / ANSWER_FAILED`; it is not an
Intake processing failure. Answer content is a non-authoritative result and
cannot become a Source Binding, Fact, Criterion, Human Decision, Goal,
Workflow, Evidence, Acceptance, or execution instruction.

An analysis, preparation, or future observation failure that can be recorded
stores one terminal Failure Record, `FAILED` status, audit, and command result
atomically. Store/audit failure cannot be reported as a successfully recorded
failure. Startup recovery runs before publishing Intake handlers and dispatches
only structurally valid non-resumable orphaned `ANALYZING` operations by their
immutable reservation operation kind. Non-Answer-only analysis orphans become
one audited `FAILED / INTERRUPTED_ANALYSIS` result. An Answer-only orphan
deterministically revalidates the exact retained prepared Admission input and
commits one `APPLIED / NO_EXECUTION / ANSWER_FAILED /
INTERRUPTED_ANSWER_DELIVERY` result without recalling the assistant. Missing or
irreproducible inputs, operation/state mismatch, and corrupt or contradictory
authority fail strict reopen.

### 5.11 Materialization and separate Start

For a current deterministic `MATERIALIZE` result, one Store transaction MUST
independently revalidate and atomically persist:

- the exact Admission Decision;
- formal Goal revision 1 validated by Goal Manager;
- one initial `DISCOVERY / READY` Workflow constructed by Workflow Runtime;
- the synchronized initial Goal lifecycle projection;
- one immutable Goal Materialization Record;
- exactly one Goal Start Authorization for `AUTHORIZE_START`, otherwise none;
- all Intake, Goal, Workflow, Admission, Materialization, and optional
  Start-Authorization audits; and
- the final Intake command outcome.

Materialization creates no Attempt, Context, Policy/Profile Workflow binding,
dispatch claim, Worker process, Candidate, Evidence, or Acceptance. After the
transaction commits, the application coordinator may submit only the
preallocated ordinary `StartGoal` command carried by the Start Authorization.
That existing command retains freshness, first Policy/Profile binding, Context,
Attempt, dispatch, idempotency, and recovery authority.

The public composite result keeps three independent dispositions:

- Answer: `NOT_REQUESTED`, `ANSWER_RETURNED`, or `ANSWER_FAILED`;
- Materialization: no Goal, materialized ready, or materialization failure; and
- Start: `NOT_AUTHORIZED`, `READY_PENDING_START`,
  `START_COMMAND_APPLIED`, `START_COMMAND_REJECTED`, or
  `START_INFRASTRUCTURE_FAILURE`.

A crash between transactions leaves a visible recoverable `READY` Goal. Replay
of the Intake outcome creates no new Intake or Materialization authority; the
coordinator may then separately resubmit only the exact preallocated Start
command. If first Start never committed, that ordinary command may perform it.
If it did commit, processed-command replay cannot recreate its bindings or
redispatch its retained Attempt. A different explicit manual `StartGoal` races
through ordinary first-Start guards and produces at most one winner. Start
failure never rolls back or misreports successful Materialization.

### 5.12 Read views, privacy, and retention

Runtime owns schema-versioned Intake status and audit views. They expose the
exact current revision/provenance chain, unresolved ambiguity, single active
question ID, specification/record digests, issuing Decision binding, and answer
schema; historical Question-to-Answer-Binding relationships; Decision reason,
Answer disposition, terminal Failure/next action, materialized Goal reference,
and separate Start disposition. They cannot issue Admission, materialize,
retry, Start, or declare technical completion.

The first local retention profile MUST be named, versioned, bounded, and
deterministic. It MUST:

- admit exact valid UTF-8 user content only within fixed byte limits;
- reject prohibited or unsupported content rather than insert synthetic
  replacement text into authoritative bytes;
- retain exact safe source bytes while dependent Admission or Materialization
  authority remains usable;
- keep redacted display and safe audit projections separate;
- exclude raw adapter exceptions, environment credentials, content rejected by
  the installed retention policy, private reasoning, unknown response fields,
  and unrestricted transcripts from authority and audit; and
- mark unavailable content explicitly so it cannot satisfy a new
  `USER_STATED` binding.

M4 may add richer privacy and erasure UX. M2.5 MUST NOT claim that a digest
alone preserves source authority after the exact permitted bytes are gone.

### 5.13 Proof layering

The mandatory M2.5 verdict is deterministic. It uses a controlled App Server
fixture through the real lower client and Intake Adapter, real SQLite
transactions/reopen, and the real existing Goal/Workflow/Start boundaries.
Model output is fixed untrusted input for repeatable authority proof.

A separate bounded live Intake Adapter observation MAY exercise the supported
installed Codex App Server. Its semantic Proposal or answer is not prescribed,
cannot replace deterministic proof, cannot self-materialize, and is not
required for the unconditional M2.5 verdict unless the milestone boundary is
explicitly amended before execution.

### 5.14 Slice 0 fixed local proof baseline

Slice 0 fixes the following identities for the bounded M2.5 implementation.
Later slices MUST use these exact IDs and versions or stop for an explicit plan
revision; they MUST NOT substitute process defaults, assistant output, project
content, or the most recently installed record.

| Binding | Exact ID | Exact version or selection |
| --- | --- | --- |
| Intent Admission Policy | `admission-policy_codeclosure-m2-5-local` | `codeclosure-m2-5-local-admission-v1` |
| denial test Admission Policy | `admission-policy_codeclosure-m2-5-test-deny` | `codeclosure-m2-5-test-deny-v1`; deterministic harness only |
| unsupported test Admission Policy | `admission-policy_codeclosure-m2-5-test-unsupported` | `codeclosure-m2-5-test-unsupported-v1`; deterministic harness only |
| Intake assistant profile | `intake-assistant-profile_codeclosure-m2-5-local` | `codeclosure-m2-5-local-assistant-v1` |
| Intake assistant adapter | `intake-assistant-adapter_codex-app-server` | `codeclosure-m2-5-intake-adapter-v1` |
| Intent-analysis response contract | `intake-response_intent-analysis` | `codeclosure-m2-5-intent-analysis-response-v1` |
| Answer-only response contract | `intake-response_answer-only` | `codeclosure-m2-5-answer-only-response-v1` |
| Intake budget profile | `intake-budget_codeclosure-m2-5-local` | `codeclosure-m2-5-local-budget-v1` |
| local principal | `principal_local-user` | trusted composition only; no CLI override |
| local retention profile | `intake-retention_codeclosure-m2-5-local` | `codeclosure-m2-5-local-retention-v1` |
| governed-execution Workflow Policy | `policy_codeclosure-m1` | `codeclosure-m1-policy-v1`; `sha256:a6a7a4e0c9930a3a79f72b5069775430d902284d31ea650e48a213a9648adcb9` |
| governed-execution Execution Profile | `profile_m1-happy-path` | `codeclosure-m1-fake-profile-v1`; `sha256:e06f2ce15169e0beac7da4998d090718a0c506d30690d2e703cdc946c77577a4` |

The existing Workflow Policy and Execution Profile are selected only for the
bounded deterministic automatic-Start proof. This does not make a fixture
alias a user-selectable Intake field, an ambient default, or a general
production execution guarantee. The separately completed M2 real-Codex path
remains a regression baseline and is not imported into or wrapped by the
Intake adapter.

The assistant profile is fixed to the M2-supported `codex-cli 0.146.0`
snapshot `sha256:0b0bdf534386d796c41596693c451aabaec2526bbac5a7965ab558edc3de8e21`,
provider `openai`, model `gpt-5.6-sol`, service tier `default`, and reasoning
effort `low`. Each operation uses one fresh process, physical Thread, and Turn
with a 300,000 millisecond terminal deadline. It selects no project tool,
shell, filesystem tool, Web Search, MCP, app, plugin, hook, skill, subagent,
dynamic tool, approval, user-input request, Compact continuation, Thread
resume, or fallback. Its operation-scoped cwd and writable state root remain
outside project, Candidate, authority, and credential roots. Model-service
transport does not grant a model-invoked network tool.

The adapter source may import only `node:buffer`, `node:crypto`, the
`node:fs` identity checks needed for real-directory isolation, `node:path`, and
`node:timers` from the Node standard library. Filesystem mutation,
child-process, network, module-loader, worker-thread, and other Node capabilities
are not adapter dependencies. Process ownership, protocol transport, and
controlled launch stay behind the lower App Server client; trusted composition
owns root creation and cleanup.

The two response contracts are strict and distinct. Their exact assistant-wire
shapes are:

```text
IntentAnalysisAssistantResponseV1
  proposedObjective?: non-blank string
  proposedCriteria: non-blank string[]
  proposedScope?: non-blank string
  proposedNonGoals: non-blank string[]
  proposedAssumptions: non-blank string[]
  proposedQuestions: non-blank string[]
  candidateSourceSpanSuggestions: CandidateSourceSpanSuggestionV1[]
  proposedClassification?: non-blank string

CandidateSourceSpanSuggestionV1
  projectionFieldRef = OBJECTIVE | REQUIRED_CRITERION | SCOPE | NON_GOAL | ASSUMPTION
  itemIndex?: non-negative integer
  rawRequestRevision: positive integer
  startByte: non-negative integer
  endByte: positive integer greater than startByte

AnswerOnlyAssistantResponseV1
  answerContent: non-blank string
```

Every array key is required even when empty. Optional scalar keys MUST be
omitted when absent; `null` is invalid. `itemIndex` is required only for
`REQUIRED_CRITERION`, `NON_GOAL`, and `ASSUMPTION`, where it addresses the
corresponding response-array item, and is forbidden for `OBJECTIVE` and
`SCOPE`. Array order is semantic, duplicate byte-identical items are rejected,
and no string is trimmed, normalized, or rewritten after decoding. Duplicate
JSON keys, unknown keys, unknown enum values, non-integer numbers, invalid
UTF-8, and values outside the fixed budgets reject the complete payload.
`proposedCriteria` contains candidate required criteria only; the bounded local
profile admits no model-authored optional-Criterion channel.

Intent analysis therefore admits only the values above. A candidate span must
address one Manifest-selected Raw Request revision using zero-based,
end-exclusive UTF-8 offsets. It is an untrusted lookup suggestion, not a Source
Binding. Answer-only admits only the single `answerContent` value. Neither
contract admits Runtime-generated identities, digests, timestamps, authority
classes, ambiguity status, trusted interaction action, Admission, Goal,
Workflow, Start, Candidate, Evidence, Acceptance, tool requests, or unknown
fields. The Coordinator authors every retained envelope and authority binding.

The fixed budget profile is:

| Limit | Value |
| --- | ---: |
| exact admitted Raw Request content per revision | 16,384 UTF-8 bytes |
| exact clarification answer per revision | 16,384 UTF-8 bytes |
| declared constraints | 16 entries, 2,048 UTF-8 bytes each |
| revisions and clarification turns per Intake Run | 8 |
| complete retained Raw Request content selected into one package | 131,072 UTF-8 bytes |
| canonical Intake Package or Answer-only Package | 262,144 bytes |
| Intake Manifest entries | 256 |
| canonical Intent-analysis response | 65,536 bytes |
| canonical Answer-only response | 131,072 bytes |
| retained Answer-only content | 16,384 UTF-8 bytes |
| proposed objective or scope | 8,192 UTF-8 bytes each |
| proposed criteria | 16 entries, 4,096 UTF-8 bytes each |
| proposed non-goals or assumptions | 16 entries per collection, 2,048 UTF-8 bytes each |
| proposed questions | 8 entries, 2,048 UTF-8 bytes each |
| proposed classification | 256 UTF-8 bytes |
| candidate source-span suggestions | 64 |
| active Clarification Questions | 1 |
| assistant terminal deadline | 300,000 milliseconds |

The lower client's stricter structural limits and 4 MiB protocol-line ceiling
remain transport backstops, not permission to enlarge an Intake semantic
budget. If required exact input cannot fit, the operation fails preparation; it
is not truncated into different authority.

The built-in Admission Policy uses this exact material-field table:

| Projection or Goal input | Local-v1 rule |
| --- | --- |
| `objective` | exactly one non-blank value is required and MUST equal bytes covered by one or more current `USER_STATED` bindings |
| `requiredCriteria[]` | 1–16 values are required; every value MUST equal bytes covered by current `USER_STATED` bindings and every materialized Criterion is `required = true` |
| `optionalCriteria[]` | fixed empty; local v1 defines no assistant or policy path that creates an optional Criterion |
| `scope.projectPath` | required for Materialization and derived only from the exact structured `declaredProjectRef` by `declared-project-to-scope_codeclosure-m2-5-v1` |
| `scope.allowedPaths[]` | fixed empty for the bounded local proof |
| `nonGoals[]` | 0–16 values; each retained value requires current `USER_STATED` bindings; an unbound model suggestion is omitted unless omission would be material, in which case it creates ambiguity |
| `assumptions[]` | MUST be empty at Materialization; every material proposed assumption requires clarification into user-stated objective, Criterion, scope, or non-goal content |
| `requestedExecutionDisposition` | derived only by `interaction-action-to-disposition_codeclosure-m2-5-v1`: `MATERIALIZE_ONLY -> LEAVE_READY`, `GOVERNED_EXECUTION -> AUTHORIZE_START`; `ANSWER_ONLY` never reaches Materialization |

Local v1 defines no model-to-authority text derivation. The two named rules
above are its complete `POLICY_DERIVED` rule set: project normalization uses the
trusted normalized structured path without reading free-form content, and the
execution-disposition rule maps the already trusted action without model
classification. A proposed field without the required exact source remains
absent or `MODEL_PROPOSED`; if that absence or proposal could change formal
intent, the Coordinator records a Material Ambiguity.

The complete ordered local-v1 rule registry is:

1. `answer-only_action_codeclosure-m2-5-v1`;
2. `abandon-active-question_codeclosure-m2-5-v1`;
3. `material-field-eligibility_codeclosure-m2-5-v1`;
4. `first-material-ambiguity_codeclosure-m2-5-v1`;
5. `materialize-only-disposition_codeclosure-m2-5-v1`; and
6. `governed-execution-disposition_codeclosure-m2-5-v1`.

Its `POLICY_DENIED` and `UNSUPPORTED` rule collections are both exactly empty.
No request-text classifier, assistant classification, free-form reason, or
Slice 1 choice may add an entry under this Policy version. Adding a production
trigger requires a new Admission Policy version and explicit plan review.

Local-v1 aggregation is first-match deterministic:

1. invalid envelopes, retention rejection, budget overflow, profile lookup
   failure, and corrupt or stale authority are typed command/preparation
   failures and do not become an Admission Decision;
2. trusted `ANSWER_ONLY` produces `PRE_ANALYSIS_NO_EXECUTION / ANSWER_ONLY`;
3. an exact eligible `ABANDON_CLARIFICATION` command produces
   `PROJECTED_NO_EXECUTION / ABANDONED` with its complete binding;
4. the material-field rule applies the fixed table above;
5. the first unresolved material field is selected in the fixed
   order project identity, objective, required Criterion, scope, assumption,
   and non-goal, with source byte order breaking ties, and produces exactly one
   `CLARIFY`; and
6. a complete `MATERIALIZE_ONLY` input produces `LEAVE_READY`, while a complete
   `GOVERNED_EXECUTION` input produces `AUTHORIZE_START` only after the exact
   Workflow Policy/Profile preflight succeeds.

Slice 1 MUST encode exactly that definition and compute its canonical digest; it
does not choose entries, predicates, or order. To prove the accepted general
`POLICY_DENIED` and `UNSUPPORTED` Decision variants without changing public
local-v1 semantics, the deterministic harness installs two exact non-default
Policies. Each copies the local registry and inserts one pre-analysis rule
immediately after `answer-only_action_codeclosure-m2-5-v1`:

| Test Policy ID / version | Sole additional rule and exact trigger | Result |
| --- | --- | --- |
| `admission-policy_codeclosure-m2-5-test-deny` / `codeclosure-m2-5-test-deny-v1` | `deny-exact-principal_codeclosure-m2-5-test-v1`; trusted `principalRef` equals `principal_fixture-policy-denied` and trusted action equals `MATERIALIZE_ONLY` | `PRE_ANALYSIS_NO_EXECUTION / POLICY_DENIED` |
| `admission-policy_codeclosure-m2-5-test-unsupported` / `codeclosure-m2-5-test-unsupported-v1` | `unsupported-governed-execution_codeclosure-m2-5-test-v1`; trusted interaction action equals `GOVERNED_EXECUTION` | `PRE_ANALYSIS_NO_EXECUTION / UNSUPPORTED` |

The fixture principal and trusted action are canonical test inputs, not
natural-language inference. These Policies are never installed by the local
CLI composition and cannot become ambient defaults.

The retention profile admits only well-formed UTF-8 containing no NUL, no
unpaired surrogate, and no C0/C1 control character other than TAB, LF, and CR.
It splits decoded content on CRLF, lone CR, or lone LF without Unicode
normalization and removes only leading/trailing ASCII SPACE/TAB for marker
matching.

The trimmed line matches a private-key marker only when it is byte-for-byte
equal to `"-----" + ("BEGIN " | "END ") + LABEL + "-----"`, using ASCII
case-sensitive bytes and one of these exact labels: `PRIVATE KEY`, `ENCRYPTED PRIVATE KEY`,
`RSA PRIVATE KEY`, `DSA PRIVATE KEY`, `EC PRIVATE KEY`, `OPENSSH PRIVATE KEY`,
and `PGP PRIVATE KEY BLOCK`. The complete field-marker set is ASCII
case-insensitive `authorization`, `proxy-authorization`, `cookie`,
`set-cookie`, `password`, `passwd`, `secret`, `client_secret`, `api_key`,
`apikey`, `access_token`, `refresh_token`, or `private_key` at the start of the
trimmed line, followed by zero or more ASCII SPACE/TAB and then `:` or `=`. No
other whitespace, case folding, substring, decoded-value, or semantic-secret
rule is implied.

Before a Raw Request revision exists, any match rejects without storing the
submitted content bytes or content digest and records only profile ID/version,
typed reason, byte count, and command/audit identity. The same classifier runs
over every decoded assistant-authored string after wire-schema validation and
before Proposal or Answer retention. Rejected Intent-analysis content commits
terminal `FAILED / RESPONSE_REJECTED`; rejected Answer-only content commits
`NO_EXECUTION / ANSWER_FAILED / RESPONSE_REJECTED`; neither retains rejected
payload bytes or a payload digest. The ordinary typed Failure/AnswerFailed
record still has its own semantic record digest. The user must safely restate
only a rejected user submission; no masking or replacement preserves source
authority.

For admitted content, the profile keeps the exact bytes while any dependent
Intake, Admission, Materialization, or Start authority remains usable. It
retains validated bounded Proposal and Answer-only values, typed failure
classifications, safe projections, and digests; it does not retain raw adapter
exceptions, environment credentials, private reasoning, unrestricted
transcripts, unknown fields, rejected submissions, or optional project
observations. Operation state is released at the terminal boundary on a
best-effort basis, but cleanup success is not authority. M2.5 adds no erasure
action and never treats a digest as retained source bytes.

Closed-schema handling is also fixed. A model-authored authority field or any
unknown key rejects the complete assistant payload without partial Proposal or
Answer retention. Authority words inside an otherwise allowed bounded string
remain inert untrusted content: they are neither stripped nor interpreted as
action, identity, Source Binding, Admission, Goal, Start, Evidence, or
Acceptance authority. Their vocabulary alone does not reject the payload;
later source-binding and Admission rules still reject any unsupported material
meaning.

The transaction shapes in Section 6 are closed without change. ADR 0034 owns
reservation/outcome replay and verified activation, the existing Goal Manager
and Workflow Runtime own atomic Goal/Workflow creation, and ordinary
`StartGoal` remains the only first-Start path. Slice 0 found no durable conflict
requiring another ADR.

## 6. Transaction map

| Operation | External work | Required atomic commit | Failure meaning |
| --- | --- | --- | --- |
| New Intake analysis reservation | none | RawRequest, revision 1, IntakeRun `ANALYZING`, command reservation, exact Manifest/external-operation bindings, audit | no assistant call before commit |
| New Answer-only reservation | none | RawRequest, revision 1, IntakeRun `ANALYZING`, command reservation, exact Answer-only Manifest/prepared Admission input/adapter/response-contract bindings, audit | no Answer-only assistant call before commit |
| Clarification reservation | none | exact current Question/Decision binding, new Raw Request revision with embedded answered-question tuple, unique immutable Answer Binding, cleared singular active reference, IntakeRun version/status, command reservation, exact Manifest/external-operation bindings, audit | stale, foreign, answered, duplicate/cross-Intake Answer Binding, Decision/spec/record mismatch, or positional input creates no new analysis |
| Deterministic command rejection | none | reservation, `REJECTED` outcome, observed Intake version, audit | no Raw Request revision, lifecycle mutation, or external call |
| Explicit abandonment | none | eligible `ABANDON_CLARIFICATION` reservation with complete reservation binding, exact current Projection/Question/issuing-Decision/Command-bound Decision, cleared active reference, `PROJECTED_NO_EXECUTION / ABANDONED`, terminal status, `APPLIED` outcome, audits; ineligible admitted command instead has one base-only reservation plus `REJECTED` outcome/audit and no Decision or lifecycle mutation | only exact-version `NEEDS_CLARIFICATION` is eligible; no Raw Request revision, assistant call, `FAILED`, or outcome-less reservation |
| Immediate deterministic no-external terminal | none | current Intake authority, any admitted Raw Request revision, reservation, closed `NO_EXECUTION` Decision, terminal status, `APPLIED` outcome, audits | no intermediate `ANALYZING`, assistant call, or split known result |
| Intent analysis | one fresh process/Thread/Turn outside Store transaction | Proposal, Manifest binding, Projection, Source Bindings, ambiguities, next Decision/status and, for `CLARIFY`, its exactly bound Question; audit and final command result | safe failure becomes terminal `FAILED`; Store failure is not recorded success |
| Answer-only | one fresh process/Thread/Turn outside Store transaction | `NO_EXECUTION` Decision, Answer-only Response, terminal status, audit, final command result | delivery failure is `ANSWER_FAILED`, not Intake `FAILED` |
| Materialization | assistant work already complete | Decision, Goal, Workflow, Materialization, optional Start Authorization, audits, final command result | no partial formal authority |
| Automatic Start | ordinary `StartGoal` effect after Materialization commit | existing first-Start transaction and processed outcome | Goal remains materialized `READY` if Start does not apply |
| Startup reconciliation | no assistant call | operation-kind-aware result: non-Answer-only analysis orphan commits Failure, `FAILED`, audit, and `FAILED` outcome; Answer-only orphan commits deterministic `NO_EXECUTION`, bounded `ANSWER_FAILED / INTERRUPTED_ANSWER_DELIVERY`, audit, and `APPLIED` outcome | missing or irreproducible input, operation/state mismatch, or corrupt authority blocks reopen |

## 7. Slice sequence

### Slice 0 — final interface and decision closure

Deliverables:

- capture the exact M2 public facade, Store, App Server client, Policy, Profile,
  CLI, migration, and dependency baseline;
- fix the built-in Admission Policy identity, assistant profile, response
  contracts, byte/count/time budgets, local principal, retention profile, and
  governed-execution Workflow Policy/Profile selection;
- adopt ADR 0034's immutable Intake command reservation/outcome contract and
  verified-activation extension without reinterpretation;
- fix the exact rejection-versus-ignore behavior for model-authored authority
  fields and authority language inside otherwise allowed proposal content;
- confirm the remaining compound transaction shapes;
- add dependency-gate expectations for the new adapter; and
- record any further durable conflict as an ADR before authority code.

Proof:

- no product code claims Intake behavior;
- every planned type has one owner and one prohibited-capability list; and
- no unresolved cross-slice decision can change authority ownership,
  persistence, retry, assistant capability, response shape, or public action
  meaning. Section 5.14 fixes the exact local Admission registry, predicates,
  and ordering; Slice 1 only encodes that definition and computes its canonical
  digest.

### Slice 1 — domain, codecs, policy, and invariant

Deliverables:

- add typed Intake identifiers and closed record unions to `packages/domain`;
- add semantic invariants, canonical projections, runtime decoders, and golden
  digest vectors, including the acyclic Question-to-Raw-Request-to-Answer-
  Binding chain, the two closed abandonment reservation shapes, and the later
  exact `AbandonmentBinding`;
- encode the already fixed local and test Admission Policy definitions, add
  installer inputs, compute their canonical digests, and add Engine contracts;
  and
- add the canonical Runtime invariant equivalent to “Intent proposals cannot
  admit or materialize themselves” together with executable invariant metadata
  and adversarial tests in the same change.

Proof:

- every unknown field, invalid scalar, enum, terminal shape, and digest drift
  fails decoding;
- Question records remain immutable, and Answer Binding uniqueness/current-
  reference invariants reject an answered active Question;
- fixed Admission input produces one semantic Decision digest;
- the local/test Policy registries, entries, predicates, ordering, and computed
  canonical digests exactly match Section 5.14; and
- no Proposal/Projection value can construct Goal or Workflow authority.

### Slice 2 — SQLite Intake authority

Deliverables:

- add complete migrations after `0025`;
- implement policy installation, command reservation/outcome, analysis,
  clarification, non-execution, failure, Materialization, and query Store ports;
- implement compound Goal/Workflow/Materialization/Start-Authorization writes;
- add startup validation, immediate-read, close/reopen, and migration preflight;
  and
- implement ADR 0034's verified SQLite activation extension so retained Intake
  project references participate in data-home/project isolation checks without
  becoming Goal authority.

Proof:

- failure injection after every new write leaves no partial authority;
- direct SQL poisoning, including missing/duplicate/cross-Intake Answer
  Bindings and missing/partial/substituted/reservation-less Abandonment
  Bindings, fails Store reopen;
- exact command replay and concurrent clarification/materialization losers create
  one respective effect; and
- existing M1/M2 rows and migrations retain their exact meaning.

### Slice 3 — packages, manifests, and Intake Assistant Adapter

Deliverables:

- add the Runtime Intake/Answer package compiler and durable Manifest;
- add `packages/adapter-codex-intake` with separate analysis and Answer-only
  operations;
- reuse only public lower-client process/protocol primitives and the four
  allowed Node built-ins;
- implement the fresh process/Thread/Turn policy, closed configuration, strict
  structured response decoding, bounded shutdown, and failure mapping; and
- add deterministic fake App Server and protocol adversarial tests.

Proof:

- the adapter imports no Worker, Candidate, Store, Goal, Workflow, Start,
  Evidence, or Acceptance capability and no Node built-in outside
  `node:buffer`, `node:crypto`, `node:fs`, `node:path`, and `node:timers`;
- no CodeClosure authority capability, configurable tool source, ambient
  instruction, project cwd, `turn/steer`, hidden retry, Thread resume, or model
  fallback is selected; the profile does not claim an empty upstream tool
  inventory; and
- observed tool use interrupts and fails the operation, while Thread loss,
  Compact, malformed/oversized output, server requests, timeout, and process
  failure produce only closed observations.

### Slice 4 — Coordinator, Projection, clarification, and Admission

Deliverables:

- implement new-Intake, clarification, and `ABANDON_CLARIFICATION`
  reservation/application commands;
- require explicit Question ID plus retained specification/record digests and
  issuing-Decision binding, with one singular active Question reference per
  M2.5 clarification turn;
- admit an optional normalized clarification project path only for an exact
  current project-identity question and carry it through pre-activation
  isolation before Runtime binding;
- validate Proposal, construct Projection revisions, Source Bindings, Material
  Ambiguities, one bounded question specification, and the acyclic
  Decision-to-Question record chain;
- construct the Question-bound Raw Request revision and unique immutable Answer
  Binding without mutating the Question;
- construct the projected abandonment Decision with its exact
  Question/issuing-Decision/Command binding without mutating the Question or
  creating a Raw Request revision;
- implement deterministic preflight and Admission over exact snapshots;
- atomically commit `CLARIFY` and non-Answer `NO_EXECUTION`; and
- expose typed in-process status views for active and non-execution runs.

Proof:

- stale revision, Proposal, Projection, source, ambiguity, project, policy, and
  concurrent mutations fail before consumption;
- model-proposed-only material fields cannot materialize;
- `READY_TO_MATERIALIZE` is never durable authority;
- bounded clarification atomically creates the bound Raw Request revision and
  unique Answer Binding, clears the active reference, and preserves immutable
  history;
- eligible abandonment atomically commits its reservation, complete terminal
  binding/Decision, cleared reference, `APPLIED` outcome, and audits, while
  stale/ineligible input creates only the base-reservation deterministic
  rejection; neither path can retain a mixed, `FAILED`, or outcome-less shape;
  and
- positional, stale, foreign, answered, Decision/spec/record-substituted, or
  unrelated question input cannot create a revision or external call; embedded
  answered-question or Answer-Binding substitution fails closed; prompt,
  affected-field, and answer-schema substitution fails closed; and free-form
  answer or assistant output cannot introduce or replace project identity.

### Slice 5 — Answer-only, terminal failure, and restart

Deliverables:

- implement the separate Answer-only operation and compound terminal result;
- implement safe terminal Failure Records and closed reason mapping;
- implement startup orphan reconciliation before Intake capability publication;
- finalize command replay behavior for active/final/failed operations; and
- add redacted status/audit projections plus the exact bounded user/assistant
  retention classifier and non-retention behavior.

Proof:

- Answer-only success/failure creates its exact terminal Intake
  Decision/Response authority but no Goal, Workflow, Evidence, Acceptance, or
  execution authority, and replay makes no second call;
- `ANSWER_FAILED` remains distinct from Intake `FAILED`;
- every exact private-key/field marker and near miss follows the fixed grammar,
  and a rejected user/assistant string retains neither its payload bytes nor a
  content/payload digest while its typed rejection record keeps its own semantic
  digest;
- a failed IntakeRun cannot reopen or call the model again; and
- orphan reconciliation creates exactly one operation-appropriate
  `INTERRUPTED_ANALYSIS` or `INTERRUPTED_ANSWER_DELIVERY` result without a model
  call, while irreproducible or corrupt authority fails reopen.

### Slice 6 — Materialization and Start composition

Deliverables:

- implement the atomic Goal Manager/Workflow Runtime Materialization primitive;
- persist the Materialization Record and optional Start Authorization;
- invoke only the preallocated ordinary `StartGoal` after commit;
- add the five closed Start dispositions and composite query; and
- preserve direct `CreateGoal` plus cancel/new-Intake correction behavior.

Proof:

- Materialization creates no Attempt or dispatch;
- each transaction rolls back its own uncommitted writes, while a later Start
  failure cannot roll back committed Materialization;
- a crash before Start leaves one visible `READY` Goal;
- automatic/manual Start races produce one first-Start winner; and
- Start substitution, replay, rejection, or infrastructure failure cannot
  rewrite Materialization.

### Slice 7 — CLI and acceptance harness

Deliverables:

- add `intake submit`, `clarify`, `abandon`, `status`, and `audit` with shared JSON/human
  Runtime views and existing exit classifications;
- add deterministic end-to-end fixtures for the required main and supplementary
  demonstrations;
- add the canonical `accept:m2.5` runner and source-identity capture;
- add fail-closed runner self-tests for stage enumeration, zero-test detection,
  source drift, evidence-manifest validation, and review-file exclusion; and
- prepare, without issuing a verdict, the README, Architecture, Goal Intake,
  Domain, Workflow, Context, Evidence, ADR index, milestone, plans index, and
  completion/non-claim wording for independent assessment.

Proof:

- handlers receive no Store, Workflow kernel, Admission issuer, or adapter
  internals;
- the CLI, deterministic fixtures, and acceptance runner pass their focused
  implementation tests without executing the milestone verdict;
- strict reopen returns the same Raw Request-to-Goal and Start-disposition
  chain; and
- no implementation Slice or runner output claims the independent M2.5 verdict.

## 8. Independent exit assessment

The exit assessment is not an implementation Slice. It begins only after
Slices 0 through 7 have completed their implementation deliverables and the
entry conditions in the
[M2.5 acceptance plan](m2.5-acceptance-plan.md) are satisfied.

The assessment sequence is:

1. execute the canonical `accept:m2.5` procedure on one exact declared source
   identity;
2. inspect its complete executable matrix, actual diff, working-tree state,
   isolation/effect trace, and evidence manifest;
3. create the independent dated M2.5 completion review under the acceptance
   plan's narrow review-file source-identity rule;
4. rerun the required documentation and offline closure checks on the
   review-bearing tree; and
5. let the independent review issue the only bounded milestone verdict.

## 9. Planning-input traceability

| Reviewed readiness item | Owning plan section/slice |
| --- | --- |
| independent implementation plan | this document |
| independent acceptance plan | [M2.5 acceptance plan](m2.5-acceptance-plan.md) |
| Intake records, state machine, transactions, SQLite migrations | Sections 5.3–5.5; Slice 2 |
| Intake Assistant Adapter and closed response contracts | Sections 5.2, 5.6–5.7; Slice 3 |
| Thread reuse, replacement, termination, and reconstruction | Section 5.6; Slice 3 |
| decide optional read-only project observation | Section 5.8: deferred from baseline |
| Admission Policy installation, digest, deterministic tests | Section 5.9; Slices 1 and 4 |
| Materialization and Start Authorization composition | Section 5.11; Slice 6 |
| CLI and separate Start-result views | Sections 5.1, 5.11–5.12; Slice 7 |
| direct `CreateGoal` and post-Materialization correction | Sections 5.1 and 5.11; Slices 6–7 |
| normal, failure, replay, concurrency, restart, and forged-authority acceptance | [M2.5 executable acceptance matrix](m2.5-acceptance-plan.md#8-mandatory-executable-acceptance-matrix) |

## 10. Slice status

| Slice | Scope | Status | Completion evidence |
| ---: | --- | --- | --- |
| 0 | final interface and decision closure | Implemented | [Slice 0 decision-closure review](../reviews/m2-5-slice0-decision-closure.md), dependency/profile preflight |
| 1 | domain, codecs, policy, and invariant | Implemented | [Slice 1 domain, policy, and invariant review](../reviews/m2-5-slice1-domain-policy-invariant.md), codec, digest, policy, and invariant tests |
| 2 | SQLite Intake authority | Implemented | [Slice 2 SQLite Intake authority review](../reviews/m2-5-slice2-sqlite-intake-authority.md), migration, transaction, corruption, activation, replay, and reopen tests |
| 3 | packages, manifests, and Intake Assistant Adapter | Implemented | [Slice 3 packages and Intake Assistant Adapter review](../reviews/m2-5-slice3-intake-packages-adapter.md); package/Manifest closure, isolated read-only lifecycle, capability-source denial, and observed-tool failure tests |
| 4 | Coordinator, Projection, clarification, and Admission | Not started | deterministic, stale, ambiguity, and source-authority tests |
| 5 | Answer-only, terminal failure, and restart | Not started | replay, delivery/failure separation, restart, and retention tests |
| 6 | Materialization and Start composition | Not started | atomicity, race, substitution, compatibility, and recovery tests |
| 7 | CLI and acceptance harness | Not started | CLI/reopen proof plus runner fail-closed self-tests; no milestone verdict |

A slice may be marked implemented only in the same change that records its
exact source identity, focused commands, zero-skip results, documentation
review, and remaining limitations. No passing assistant response, isolated
unit test, migration skeleton, CLI rendering, or plan checkbox is slice or
milestone completion.

## 11. Milestone completion rule

The independent review may issue the bounded M2.5 `PASS` only after:

1. Slices 0 through 7 are implemented with their focused evidence;
2. the canonical `accept:m2.5` procedure runs on one exact source identity;
3. every mandatory row passes with no waiver, expected failure, or hidden
   assistant retry;
4. every M1 and M2 regression gate remains green;
5. strict SQLite reopen proves the complete Intake-to-Goal chain;
6. documentation describes implemented and planned behavior consistently;
7. the review-bearing tree passes the required offline and source-closure
   checks; and
8. the independent review finds no blocker, unavailable mandatory evidence, or
   conditional waiver.

No implementation Slice, acceptance runner, fixture, assistant response, or
stored Goal result may issue that milestone verdict.

That verdict proves only the bounded M2.5 Goal Intake and Materialization
vertical slice. It does not prove project-assisted Intake, complete
requirements understanding, Goal revision, rich conversation routing, product
completion, production readiness, or external-effect authority.
