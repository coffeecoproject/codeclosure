# M2.5.1 Real Intake-to-Codex Composition Closure completion review

- Review date: 2026-08-13
- Reviewed branch: `m2.5-goal-intake`
- Reviewed base Git revision: `dfe47987f0147c9b3695555f6edbea95c58e531d`
- Executable assessment: `PASS / READY_FOR_INDEPENDENT_REVIEW`
- Independent milestone verdict: `PASS`
- Review authority: bounded M2.5.1 milestone verdict only

## 1. Review outcome

The canonical M2.5.1 executable assessment passed all 13 mandatory stages and
all 68 matrix rows on one exact source identity. It reported zero failed,
cancelled, skipped, todo, waived, expected-failure, unexpected-not-applicable,
source-drift, or unexplained-warning result. Independent validation of the
manifest, artifacts, source closure, real composition receipts, implementation
boundaries, and current-source regressions found no unresolved mandatory row,
fixture substitution for Live evidence, or higher-authority conflict.

The independent milestone verdict is therefore an unconditional `PASS` for the
bounded question in the
[M2.5.1 acceptance plan](../plans/m2.5.1-acceptance-plan.md#3-acceptance-question-and-bounded-claim):
the pinned real Codex App Server supports bounded Answer-only and Intent-analysis
Intake, and the trusted composition can Materialize a source-bound Goal whose
ordinary Start uses real Codex for candidate-free `DISCOVERY` and `PLAN`, real
Candidate-bound `IMPLEMENT`, protected verification, Evidence, technical
Acceptance, closeout, cleanup, and strict reopen without transferring CodeClosure
authority to the model or Adapter.

This verdict completes the bounded M2.5.1 milestone review. It does not complete
CodeClosure, implement M2.6, or issue release, deployment, promotion, or other
external-effect authority.

## 2. Audited source and evidence identity

The canonical runner declared this review file as its sole self-referential
source exclusion before execution. Every other tracked or untracked,
non-ignored source path participated in the ordered source manifest.

| Field | Reviewed value |
| --- | --- |
| Branch | `m2.5-goal-intake` |
| Base Git revision | `dfe47987f0147c9b3695555f6edbea95c58e531d` |
| Opening working tree | Clean |
| Closing working tree | Clean |
| Source manifest schema | `codeclosure-source-manifest-v1` |
| Source manifest paths | 1,095 |
| Opening/closing source digest | `sha256:7fec7a1ca0f08a6a626dfa901185d03bb9cd2e2c978a75143baaddde22180e2c` |
| Review exclusion | `docs/reviews/m2.5.1-completion-review.md` |
| Opening/closing identity | Exact match |
| Evidence manifest SHA-256 | `75a6d437dfea31bb74c07962d103d6415282e61bc63147f455d1491110cd740d` |
| Matrix contract digest | `sha256:59b16bf11c3083005b8482de9f5298fbc5e9b44f38d3c3ffbdd55849de58e6a1` |
| Live authorization | Explicit |

The machine-readable evidence directory used for this review was:

`/private/var/folders/hj/1s6hbmdx7tz1g9cc4dc1ykvc0000gn/T/codeclosure-m2-5-1-acceptance-NAFMKx`

An independent invocation of `validateM251EvidenceManifest` accepted the
manifest and every referenced artifact digest and returned exactly
`PASS / READY_FOR_INDEPENDENT_REVIEW`, 13 stages, 68 rows, and the sole review
exclusion above.

The review-bearing tree may differ from the reviewed source only by this
excluded review file. Any other source change invalidates the identity above
and requires validation appropriate to that change.

## 3. Environment and frozen contracts

| Bound item | Reviewed value |
| --- | --- |
| Node.js | `v22.22.3` |
| pnpm | `11.1.3` |
| Lockfile digest | `sha256:fe24fc23fae3b4cb17f373b67e95df50be11191741e090a59b32e1451eaf7eb2` |
| Slice 0 contract | `codeclosure-m2-5-1-slice0-v7` / `sha256:f0c14002d8df71a979587bd95cb662c4751291e8349eeeb36208a6f9a1c9e090` |
| Selected Codex | `codex-cli 0.146.1` |
| Selected protocol snapshot | `sha256:312156edfdf765f134ce5f754419a9509fd34186798a1bdbb0c219ac7c19c610` |
| Historical Codex identity | `codex-cli 0.146.0` retained with its historical meaning |
| Demonstration Git commit | `1ad4bbb383d8ef38e29400b2625810ffa9dfb953` |
| Demonstration Git tree | `1e28086507ea644175065aaf6f4a8e2acce05036` |
| Formal Execution Profile | `profile_m2-5-1-contained-real-codex` / `codeclosure-m2-5-1-contained-real-codex-profile-v2` |
| Protected Check | `check_m2-5-1-payment-idempotency` / `codeclosure-m2-5-1-payment-idempotency-v1` |
| Protected Check digest | `sha256:16a4a1c6c18dddc972cd8e51507f4e22d1c78b172da871687bca1d9d44f604af` |

The selected `0.146.1` identity applies only to new M2.5.1 operations. The
historical M2 and M2.5 `0.146.0` records retain their accepted meaning; the
assessment did not rewrite, rehash, silently recall, or substitute them.

## 4. Reviewed authority sources and architectural decisions

The review applied the repository authority order across:

- [`RUNTIME_INVARIANTS.md`](../../RUNTIME_INVARIANTS.md),
  [`PRODUCT.md`](../../PRODUCT.md), and
  [`ARCHITECTURE.md`](../../ARCHITECTURE.md);
- [Goal Intake](../goal-intake.md), [Domain Model](../domain-model.md),
  [Workflow](../workflow.md), [Context Compiler](../context-compiler.md),
  [Evidence Model](../evidence-model.md), and
  [Acceptance Engine](../acceptance-engine.md);
- the [M2.5.1 implementation
  plan](../plans/m2.5.1-real-intake-codex-composition-closure.md),
  [acceptance plan](../plans/m2.5.1-acceptance-plan.md), and
  [milestone boundary](../milestones.md#m251--real-intake-to-codex-composition-closure);
- accepted
  [ADR 0043](../adr/0043-candidate-free-codex-project-read-authority.md) and
  [ADR 0044](../adr/0044-source-freeze-owned-candidate-change-containment.md); and
- the historical [M2 completion review](m2-completion-review.md),
  [M2.5 completion review](m2.5-completion-review.md), M2.5.1 slice reviews,
  and the
  [first canonical-assessment diagnostic](m2.5.1-first-canonical-assessment-diagnostic.md).

The implementation follows the accepted decisions: Runtime remains the only
Workflow writer, Candidate Manager owns source observation and Candidate
freeze, Acceptance Engine alone issues technical `ACCEPT`, and Codex protocol
types remain outside Domain and Workflow authority. No durable implementation
decision differs from the accepted ADR set, so this review requires no new ADR.

## 5. Canonical executable stages

The explicitly authorized canonical `accept:m2.5.1` procedure produced the
following closed results. The counts are the stage-owned executable counts;
all have zero failure, cancellation, skip, todo, waiver, expected failure,
unexpected not-applicable, source drift, and unexplained warning.

| Stage | Result | Passed | Artifact digest |
| --- | --- | ---: | --- |
| `preflight` | PASS | 2/2 | `sha256:68e38899d6e7b8e4ff846625048b399e1d9b2e8f1d75b6a2b17e158b91302798` |
| `quality` | PASS | 1,417/1,417 | `sha256:cc9b269f859ec3078e64a14ebdaf7200eb005a18239dd7e3fd3dba1c7dfa6aa8` |
| `v1-compatibility-and-reopen` | PASS | 21/21 | `sha256:92f2f05f9ab18f8bbab77d6290c9b60d435490e5e9e8c13c180058714cdb5ae6` |
| `intake-protocol-and-failure-closure` | PASS | 56/56 | `sha256:b1e6739d8c1a5f87ea7c9fccd35f753e7df0a6990b748b0ecc5ca82758bb4603` |
| `live-intake` | PASS | 6/6 | `sha256:f74ed276ead766c826f3abe2f04fd3edf127dbaef4f07521e44bcc4f3bc3143c` |
| `profile-and-start-binding` | PASS | 35/35 | `sha256:a8d00afa4e613c1c1fa8e385eda2620f1b4f5cea8995db21fe00397a55b81ae9` |
| `deterministic-composition-and-recovery` | PASS | 57/57 | `sha256:38df01c9b197b43d2c83889a3239fe06a2c106db1847b08dae76c30ec00d4f0f` |
| `live-intake-to-codex` | PASS | 7/7 | `sha256:2ef13cd00833dfe9f7182b29fdaba1fe9643ababe5cf24514d19b8381d131cf9` |
| `candidate-and-acceptance-integrity` | PASS | 64/64 | `sha256:b3f2e863cd13048e336ac1d6695397980fb28be6f7ffd676ca98c69eeffce4c9` |
| `m1-regression` | PASS | 1,417/1,417 | `sha256:af95c8904135cba7819751dce84adf3ccf84c1c35a5e1e2c918e59856109529f` |
| `m2-current-source-regression` | PASS | 1,417/1,417 | `sha256:7971f04460a255562f11432e64a881718c2c729cbe42eb83d52f4f2be7a31650` |
| `m2.5-current-source-regression` | PASS | 71/71 | `sha256:042f1575dd383cb4a90f68501b5729a7c1193e1afd28270c8aacb1d392a6bbbf` |
| `source-closure-and-manifest` | PASS | 75/75 | `sha256:35f7e02fef7f8a21d8ee5d49d934d32ca9a3f4671ede35f090ab76b6977edb29` |

The final passing run on `dfe4798` used no within-run semantic retry. Earlier
canonical attempts on earlier source revisions failed closed and led to source
corrections recorded in Git; they are diagnostics, not waivers or evidence
substituted into this final identity.

## 6. Mandatory matrix review

The manifest reproduces each acceptance-plan row, exact proof owner, owning
stage, row-contract digest, artifact digest, and evidence-receipt digest. The
independent review checked those bindings and the owning implementation paths.

| Matrix group | Result | Reviewed rows |
| --- | --- | --- |
| Environment and static authority | 7/7 PASS | `M251-E01` through `M251-E07` |
| Intake protocol and Observer | 10/10 PASS | `M251-P01` through `M251-P10` |
| Version and reopen compatibility | 6/6 PASS | `M251-V01` through `M251-V06` |
| Mandatory real Intake | 6/6 PASS | `M251-L01` through `M251-L06` |
| Trusted composition | 11/11 PASS | `M251-C01` through `M251-C11` |
| Real execution and authority | 11/11 PASS | `M251-X01` through `M251-X11` |
| Failure, replay, and cleanup | 11/11 PASS | `M251-F01` through `M251-F11` |
| Regression and source closure | 6/6 PASS | `M251-R01` through `M251-R06` |

No row was satisfied by marker presence alone. Test-owned rows were bound to
the emitted TAP names; Live rows were bound to validated metadata-only
receipts; regression rows consumed their closed nested results; and source
closure rebound opening/closing manifests plus all artifact digests.

## 7. Real Intake and linked composition evidence

The mandatory Live Intake stage executed three fresh operations through the
corrected versioned Adapter:

- Answer-only returned `NO_EXECUTION / ANSWER_RETURNED`, with no Goal,
  Materialization, Start Authorization, or project observation;
- clear Intent analysis returned a schema-valid proposal whose exact retained
  values were rebound by Runtime rather than trusted from model provenance; and
- ambiguous Intent analysis entered deterministic clarification and created no
  Goal.

The operations used separate processes, Threads, Turns, and owned roots. They
reported no forbidden effect, project observation grant, assistant retry,
Thread reuse, Compact, source mutation, raw diagnostic content, or credential
content.

The causally linked Live composition then passed all 11 ordered stages:

`PREFLIGHT -> INTAKE_CLARIFICATION -> MATERIALIZATION -> ORDINARY_START -> DISCOVERY -> PLAN -> IMPLEMENT -> PROTECTED_VERIFICATION -> EVIDENCE_ACCEPTANCE_CLOSEOUT -> STRICT_REOPEN -> CLEANUP`.

Its ordinary Start resolved the exact installed
`profile_m2-5-1-contained-real-codex` Profile. `DISCOVERY` and `PLAN` each used
real Codex over exact `PROJECT_READ` authority and returned only proposals;
`IMPLEMENT` used real Codex over the controlled Candidate and returned a
completion request. Every phase rechecked source currency, recorded no
forbidden activity, and reported `fakeFallbackUsed=false`.

Candidate Manager created Candidate authority only at the governed
`PLAN -> IMPLEMENT` transition. Freeze schema version 2 retained one modified
allowed path, two equal stable source digests, and the exact change-set and
allowed-path-policy digests. The protected Check passed over that frozen
Candidate. Runtime-owned Evidence bound the same Candidate and Check, the
Acceptance Engine issued `ACCEPT`, Workflow closed, and strict reopen returned
the retained result without assistant recall or Worker redispatch.

The separately authorized effective-containment probe passed candidate-free
selected-source reads and snapshot-write denial, Candidate-bound write,
content-free denied-boundary opens, zero network, zero approvals, and cleanup.
That probe is assessment evidence only; it does not replace Runtime, Candidate
Manager, Store, Evidence, or Acceptance authority.

## 8. Authority, isolation, and Fake seam findings

Independent source inspection and the matrix evidence establish that:

- the lower App Server client decodes raw protocol, the Intake Adapter-local
  Projection is the sole successful decoded-notification consumer above it,
  and the Observer consumes only the closed normalized event union;
- Codex notifications, Thread/Turn identity, model output, Adapter observation,
  activity classification, and assessment receipts do not become Domain,
  Workflow, Goal, Candidate, Evidence, Acceptance, or closeout authority;
- Runtime owns phase driving and Workflow mutation, Candidate Manager owns
  project-read/Candidate effects and freeze-v2 observation, Store independently
  enforces retained relationships, and Acceptance Engine alone issues technical
  `ACCEPT`;
- candidate-free Codex receives only the exact Runtime-owned selected-source
  snapshot and cannot read the source checkout, Candidate, authority,
  credentials, protected assets, projection-excluded paths, or sibling
  snapshots;
- `IMPLEMENT` writes only the mutable Candidate and remains contingent on
  Candidate Manager-owned stable freeze-v2 changed-path proof; and
- `FakeWorker`, `FakeCandidateSource`, and `FakeVerificationRunner` remain
  explicit historical/test infrastructure. They do not appear in the formal
  Profile, selected phase resolver, mandatory Live path, or fallback path.

The real path is deliberately exposed as the bounded supported M2.5.1
composition and explicitly authorized assessment command. Adding the persistent
natural-language foreground and general user-facing routing remains M2.6
scope; its absence is not filled by a second M2.5.1 authority or a hidden
fallback.

## 9. Failure, restart, regression, and privacy closure

Deterministic evidence covers the original Intake protocol regression,
malformed/unknown/forbidden protocol activity, provisional-terminal ordering,
process failure, timeout and interruption, identity substitution, source and
snapshot drift, Plan/source mismatch, phase-incompatible activity, Candidate
containment failure, protected-verification rejection, crash windows, exact-
grant cleanup reconciliation, cross-process cleanup concurrency, strict reopen,
and replay without model recall.

The failure paths discard untrusted results, create no replacement authority,
do not fall back to FakeWorker, and cannot convert a Worker completion request,
command notification, Candidate diff, or green command into Acceptance.

Current-source regression retained the accepted boundaries:

- M1 remained green through its complete quality/acceptance baseline;
- M2 current-source regression, including its real Worker evidence, passed
  without reissuing the historical M2 verdict; and
- all 71 historical M2.5 rows passed without claiming that the historical M2.5
  review contained mandatory Live Intake evidence.

The manifest retains no user or assistant/model content, credential or account
content, reasoning or transcript, raw protocol, raw exception, source bytes, or
unrestricted command output. The Live receipts independently report the same
privacy closure, source immutability, owned-process shutdown, and removal of
assessment, Intake, Worker, ProjectRead, Candidate, verification, and authority
roots.

## 10. Review scope, history, and engineering quality

The implementation range after the historical M2.5 completion review contains
58 commits and 220 changed files. Review was organized by the frozen proof-owner
map and authority boundaries rather than treating line count as evidence. The
changes are limited to the M2.5.1 compatibility/composition closure, its
accepted project-read and Candidate-freeze decisions, tests, Live assessment,
and directly affected documentation.

The late assessment corrections were reviewed for root cause and owning layer:

- stage-resource limits are owned once by the assessment library and bound
  into command identity rather than scattered through stage consumers;
- M2.5 proof-owner alignment follows the renamed current-source dependency
  owner and has a direct regression test;
- restart-proof SQLite contention is represented as a bounded read-only proof
  observation under the existing proof deadline and does not alter Runtime or
  Store authority; and
- the M2 Seatbelt test fixture receives bounded startup headroom while product
  verification contracts and dedicated timeout-adversarial coverage remain
  unchanged.

No correction bypasses an abstraction, duplicates policy, creates another
source of truth, changes a product authority contract to make the harness pass,
or leaves a known structural follow-up required for M2.5.1 correctness. No
broader refactor is required for this milestone.

## 11. Documentation and review-file closure

The reviewed source consistently described the state that existed before its
canonical run: Slice 5 implementation was present, while a clean-source rerun
and independent verdict were still pending. The successful assessment is
post-source evidence, so the runner correctly did not mutate those documents.
The automated `M251-R05` owner proved Markdown structure, links, and source
closure; this independent review additionally checked the semantic state and
found the remaining wording to be review sequencing rather than a conflicting
implementation claim.

Following the established M2.5 completion pattern, this sole excluded file
records the independent verdict without changing the reviewed identity. A
separate post-review documentation-only change must synchronize README,
`AGENTS.md`, Architecture, relevant domain status sections, plan/review indexes,
and milestones to record M2.5.1 completion and remove the M2.6 prerequisite
block. Historical diagnostics and slice reviews must preserve their dated
claims. That synchronization cannot reinterpret the reviewed source or create
another completion authority.

Review-file closure checks are recorded after this file is created:

| Check | Result | Bounded meaning |
| --- | --- | --- |
| `corepack pnpm docs:check` | PASS | 75/75 tests and 112 portable GFM sources, links, anchors, paths, and README status ownership passed |
| `node scripts/source-identity.mjs --review-exclusion docs/reviews/m2.5.1-completion-review.md` | PASS | 1,095 paths and `sha256:7fec7a1ca0f08a6a626dfa901185d03bb9cd2e2c978a75143baaddde22180e2c`; review-bearing tree differs only by the declared exclusion |
| Independent `validateM251EvidenceManifest` invocation | PASS | 13 stages, 68 rows, all artifacts and exact exclusion validated |
| `git diff --check` | PASS | No whitespace error in the review change |
| Exact path/status inspection | PASS | Sole working-tree change is this review file |

The canonical Live assessment must not be rerun merely to add the declared
review exclusion. Any change outside this file instead requires validation
matched to that change and cannot be silently attributed to the reviewed
manifest.

## 12. Residual non-claims and final verdict

This review does not:

- rewrite or reissue the historical M2 or M2.5 verdict;
- turn a model or Adapter result into technical Acceptance;
- implement M2.6 Frontstage or M2.7 Host behavior;
- prove arbitrary Codex-version, model, or project compatibility;
- prove arbitrary-project verification completeness or automatic repair;
- authorize source-project promotion, merge, release, deployment, or another
  external effect; or
- claim CodeClosure product completion.

Final verdict: `PASS`.

Every mandatory M2.5.1 row passed on the exact reviewed source with explicit
Live authorization and exact source/evidence closure. This completes the
bounded M2.5.1 milestone review. M2.6 remains unimplemented and may begin only
after the post-review status synchronization records this prerequisite without
changing M2.5.1 authority.
